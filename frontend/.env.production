# Production build-time config for the frontend.
# These are PUBLIC client-side values (they ship in the browser bundle either
# way — the anon/publishable key is designed to be public; RLS protects data).
# The SECRET keys (service role, encryption) are NOT here — they live only as
# Cloud Run environment variables on the backend.

VITE_SUPABASE_URL=https://cycqtqzfjlprsjwilizn.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_w-AFBLsGBpGh5DBm9CABZg_VhA0fNYs

# Empty = same-origin: the frontend calls /api/... on the same domain that
# serves it (one Cloud Run service serves both).
VITE_API_URL=
